#pragma once

#define vector std::vector
typedef unsigned size_t;  //general
typedef unsigned blockSize_t; //block
typedef unsigned codeSize_t; //code
typedef unsigned sDataSize_t; //script data

#define invalidIndex (size_t)-1
#define invalidBlock (blockSize_t)-1
#define invalidCode (codeSize_t)-1
#define invalidsData (sDataSize_t)-1
